# codemagic.yaml
workflows:
  ios-workflow:
    name: iOS Workflow
    max_build_duration: 60
    instance_type: mac_mini_m1  # or mac_mini_12_cores, etc.
    environment:
      flutter: stable
      xcode: latest
    scripts:
      # Pre-Build script that replaces your old iOS script 
      - name: Pre-Build Setup
        script: |
          #!/usr/bin/env bash
          set -e

          echo "=== [Step 0] Flutter Doctor Check ==="
          flutter doctor -v

          echo "=== [Step 1] Precache iOS Artifacts ==="
          flutter precache --ios

          echo "=== Checking if iOS artifacts exist in Flutter cache ==="
          ls -la "$(dirname "$(which flutter)")"/cache/artifacts/engine/ios || true

          echo "=== [Step 2] Flutter Clean & Pub Get ==="
          flutter clean
          flutter pub get

          echo "=== [Step 3] Navigate to iOS Folder ==="
          cd ios

          echo "=== [Step 4] Show Original Podfile for Debug ==="
          cat Podfile || true

          echo "=== [Step 5] Remove existing platform lines and post_install block ==="
          # Remove lines containing 'platform :ios'
          sed -i '' '/platform :ios/d' Podfile

          # Remove lines from 'post_install do' up through the next standalone 'end'
          sed -i '' -e '/^post_install do/,/^end$/d' Podfile

          echo "=== [Step 6] Append New Platform Line and Post-Install Block ==="
          cat >> Podfile <<'EOL'

platform :ios, '13.0'

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
    if target.platform_name == :ios
      target.build_configurations.each do |config|
        config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'
      end
    end
  end
end
EOL

          echo "=== [Step 7] Final Podfile ==="
          cat Podfile

          echo "=== [Step 8] Remove Old Pods and Lockfile ==="
          rm -rf Pods Podfile.lock

          echo "=== [Step 9] Update Repo and Install Pods ==="
          pod repo update
          pod install

          echo "=== [Step 10] Return to Project Root ==="
          cd ..

          echo "=== [Step 11] Check App Icon Set ==="
          ls -l ios/Runner/Assets.xcassets/AppIcon.appiconset/ || true

          echo "=== [Step 12] Run Flutter Launcher Icons ==="
          flutter pub run flutter_launcher_icons:main

          echo "=== [Step 13] Set Build Number ==="
          FLUTTER_BUILD_NUMBER=16
          export FLUTTER_BUILD_NUMBER
          echo "Build Number set to: $FLUTTER_BUILD_NUMBER"

          echo "=== [Done] Pre-Build Setup Completed Successfully ==="

      # Main build script
      - name: Build iOS .ipa
        script: |
          flutter build ipa --release

    artifacts:
      - build/ios/ipa/*.ipa

    publishing:
      app_store_connect:
        # Everything is inlined here
        api_key:
          key_id: "4P3J9NG42Y"
          issuer_id: "fdc7b1c2-d82e-45d6-8ade-1d380db54d65"
          private_key: |
            -----BEGIN PRIVATE KEY-----
            MIGTAgEAMBMGByqGSM49AgEGCCqGSM49AwEHBHkwdwIBAQQgfjhltNSaiRRAEADT
            7Vz9FIDYeUWaY0xq8t86uUBTzeygCgYIKoZIzj0DAQehRANCAASoMAus06DEkKK4
            zGMW8iVmhvB7lWoGow6/WDNpUhmGwv27Z83t+PCavbvU6ec+1eJvjfzvc5KoLPpR
            qujhBlmh
            -----END PRIVATE KEY-----
        export_method: app-store
